
Import('env')

sources = [
           'register_types.mm',
           'ios/src/godotRemoteConfig.mm'
           ]

if (env["platform"] == "iphone"):
    env.add_source_files(env.modules_sources, sources)

